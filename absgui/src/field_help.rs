//! Short explanations shown under form fields (kernel + ABS settings pages).

macro_rules! help {
    ($($name:ident),+ $(,)?) => {
        $(
            pub fn $name() -> &'static str {
                abs_i18n::t(concat!("gui.help.", stringify!($name)))
            }
        )+
    };
}

help!(
    cpusched,
    processor_opt,
    llvm_lto,
    hz_ticks,
    tickrate,
    preempt,
    hugepage,
    cc_harder,
    lto_suffix,
    gcc_suffix,
    kcfi,
    source,
    build_env,
    package_alias,
    package_compiler,
    package_tests,
    package_upstream_github,
    package_upstream_prereleases,
    package_pre_update_cmd,
    package_post_update_cmd,
    package_custom_local_cmd,
    package_custom_chroot_cmd,
    ramdisk_targets,
    ramdisk_w,
    ramdisk_c,
    ramdisk_p,
    ramdisk_r,
    kernel_ramdisk_targets,
    kernel_ramdisk_w,
    kernel_ramdisk_p,
    kernel_ramdisk_r,
    kernel_ramdisk_hint,
    pgo_benchmark_workdir,
    pgo_enabled,
    pgo_archive_dir,
    pgo_save_kernels_dir,
    pgo_benchmark,
    pgo_profiling_quality,
    pgo_compare_preset,
    pgo_build_user,
    pgo_sysctl,
    pgo_auto_restart,
    pgo_preset,
    pgo_profile_scratch,
    pgo_perf_data_on_ram,
    pgo_propeller_profiles_on_ram,
    pgo_convert_relocate,
    pgo_verify_boot,
    pgo_select_boot_kernel,
    pgo_compare_current,
    pgo_compare_debug_clean,
    pgo_compare_autofdo_clean,
    pgo_compare_final,
    pgo_reboot_before_start,
    pgo_shutdown_after_finish,
    pgo_reuse_afdo_profile,
    pgo_reuse_propeller_profile,
    pgo_skip_propeller,
    pgo_perf_event_args,
    pgo_perf_extra_args,
    pgo_vmlinux,
    pgo_afdo_tool,
    pgo_propeller_tool,
    pgo_afdo_profile_name,
    pgo_state_file,
    path_packages,
    path_chroot,
    path_ready,
    path_chroot_makepkg,
    default_env,
    default_compiler,
    concurrent_repos,
    concurrent_compilations,
    system_update_first,
    ignore_failures,
    compile_first_install,
    clean_install_default,
    ignore_already_made,
    fast_aur_rpc,
    clean_chroot_after,
    global_cpu_threads_mode,
    global_cpu_threads_cap,
    maximum_cpu_threads_cap,
    default_compilation_threads,
    package_compilation_threads,
    package_compile_alone,
    package_compilation_priority,
    check_update_startup,
    auto_update_startup,
    self_update_at_updates,
    install_testing,
    self_update_install,
    self_update_use_pacman,
    install_absgui,
    manual_update,
    skip_install,
    skip_install_after,
    use_separate_skip_after,
    sys_repos_cmd,
    sys_full_cmd,
    sys_no_refresh_cmd,
    sys_auto_refresh_delay,
    sys_remember_sudo,
    sys_ignore_flag,
    sys_ignore_packages,
    held_packages,
    held_name,
    held_version,
    held_triggers,
    held_check,
    ramdisk_enabled,
    ramdisk_mount,
    ramdisk_size,
    ramdisk_mode,
    ramdisk_global_w,
    ramdisk_global_c,
    ramdisk_global_p,
    ramdisk_seed,
    ramdisk_sync,
    ramdisk_min_free,
    ramdisk_zram,
    package_zram,
    ramdisk_warn_packages,
    ramdisk_reclaim,
    repo_url,
    compiler_cc,
    compiler_cxx,
    terminal_colors,
    terminal_lines_limit,
    log_save,
);
