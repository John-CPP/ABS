post_install() {
  echo "ABS installed. Run abs (it will offer the config wizard if you have no abs.toml)."
  echo "The wizard asks whether to keep AbsGui; that choice is reused on --self-update."
  echo "  abs --config-wizard"
  echo "  abs --configure"
}

post_upgrade() {
  post_install "$@"
}
